services:

    test.client:
        class: Symfony\Bundle\FrameworkBundle\Client
        arguments:
            - "@kernel"
            - "%test.client.parameters%"
            - "@test.client.history"
            - "@test.client.cookiejar"
        shared: true
        public: true